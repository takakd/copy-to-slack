version: 2.1
orbs:
  node: circleci/node@4.0
  # Ref. https://github.com/CircleCI-Public/slack-orb
  slack: circleci/slack@4.0
jobs:
  build-test-and-notify:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm test
      - slack/notify:
          event: fail
          template: basic_fail_1
      - slack/notify:
          event: pass
          template: success_tagged_deploy_1
workflows:
  build-and-notify:
    jobs:
      - build-test-and-notify
